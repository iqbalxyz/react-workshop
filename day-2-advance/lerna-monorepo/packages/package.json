{
  "name": "todo-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^6.1.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "lerna clean",
    "dev": "lerna run start --parallel",
    "start": "lerna run start --scope=@todo-app/main-app"
  }
}
